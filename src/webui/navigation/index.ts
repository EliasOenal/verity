// Navigation is mostly handled internally by the framework.
// All the app really needs is the NavItem definition.
// Let's also pass it the NavigationController itself, though it strictly
// doesn't even need that.
export { NavItem } from "./navigationDefinitions";
export { NavigationController } from "./navigationController";
